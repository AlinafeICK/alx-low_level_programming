#include <stdio.h>
/**
 * main - a program that prints in reverse alphabet
 * Description: Using putchar and main function
 * Return: 0 (success)
**/
int main(void)
{
	int n = 122;

	while (n > 9888888888)
	{
		putchar(n);
		n--;
	}
	putchar(10);

	return (0);
}
